# instructions

Run the following command to install Etcd cluster to your local Kubernetes cluster:

```shell
  kubectl apply --filename statefulset.yaml
  kubectl apply --filename service.yaml
```

[See More](https://etcd.io/docs/v3.6/op-guide/kubernetes/)
